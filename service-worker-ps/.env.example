# Example environment configuration for Service Worker PostgreSQL
# Copy this file to .env and adjust values as needed

# NATS Configuration
NATS_URL=nats://localhost:4222
NATS_SUBJECT=inventario.cuadrilla

# Database Configuration
# PostgreSQL example:
DATABASE_URL=postgres://gridflow_user:gridflow_password@localhost:5432/gridflow?sslmode=disable
# Oracle example (not implemented yet):
# DATABASE_URL=oracle://user:password@host:1521/service
# MongoDB example (not implemented yet):
# DATABASE_URL=mongodb://user:password@host:27017/database

# Repository Type (postgresql, oracle, mongodb)
REPOSITORY_TYPE=postgresql

# Worker Pool Configuration
WORKER_NUM_WORKERS=10           # Number of concurrent workers
WORKER_BUFFER_SIZE=100          # Buffer size for message queue
WORKER_SHUTDOWN_TIMEOUT=30      # Graceful shutdown timeout in seconds
